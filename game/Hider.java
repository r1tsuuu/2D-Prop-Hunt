package game;

import math.Vector2;

public class Hider extends Character {
    public Hider(String name, int w, int h, int fps, Vector2 position, String path) {
        super(name, w, h, fps, position, path);

    }
}
